{{!--
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="./account.css">
    <title>Xác thực bằng OTP</title>
</head>

<body>
    <div id="center-form">
        <div class="container">
            <div class="wrap"> --}}
                <form action="#" method="post" id="otpForm">
                    <h2>Xác thực bằng OTP</h2>
                    <h3>Vui lòng nhập mã 6 chữ số đã gửi đến email {{email}} của bạn để xác minh.</h3>

                    <div class="information i-rows">
                        <input class="input" type="text" inputmode="numeric" maxlength="1" id="otp1" />
                        <input class="input" type="text" inputmode="numeric" maxlength="1" id="otp2" />
                        <input class="input" type="text" inputmode="numeric" maxlength="1" id="otp3" />
                        <input class="input" type="text" inputmode="numeric" maxlength="1" id="otp4" />
                        <input class="input" type="text" inputmode="numeric" maxlength="1" id="otp5" />
                        <input class="input" type="text" inputmode="numeric" maxlength="1" id="otp6" />
                    </div>

                    <div class="forgot-remember-me">
                        <p>Gửi lại mã sau: 15s</p>
                        <a href="#"><strong>Gửi lại OTP </strong></a>
                    </div>

                    <div class="active-button">
                        <p id="verifyBtn" style="cursor: pointer;">Xác nhận OTP</p>
                    </div>

                    <a href="/account/login" class="login"><strong>Trở về đăng nhập</strong></a>
                </form>
                <script src="/js/account.js"></script>
                <script>
                    document.getElementById("verifyBtn").addEventListener("click", function () {
                        // Lấy giá trị từ các input
                        const otp = Array.from(document.querySelectorAll("#otpInputs .input"))
                            .map(input => input.value)
                            .join("");

                        // Kiểm tra nếu OTP không đầy đủ
                        if (otp.length !== 6 || isNaN(otp)) {
                            alert("Vui lòng nhập đủ 6 chữ số!");
                            return;
                        }

                        // Chuyển hướng đến route xác minh OTP
                        window.location.href = `/verifyOTP?otp=${otp}`;
                    });

                </script>
                {{!--
            </div>
        </div>
    </div>
    <script src="./account.js"></script>
</body>

</html> --}}